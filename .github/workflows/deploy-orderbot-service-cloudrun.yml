name: 'Build and Deploy Orderbot Service to Cloud Run'

on:
  push:
    branches: [ "main" ]
    paths:
      - "orderbot/**"
      - ".github/workflows/deploy-orderbot-service-cloudrun.yml"
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: 'read'
      id-token: 'write'
    uses: ./.github/workflows/deploy-cloudrun-template.yml
    with:
      source_directory: 'orderbot'
      flags: '--allow-unauthenticated'
    secrets:
      service_name: ${{ secrets.ORDERBOT_SERVICE_NAME }}
      region: ${{ secrets.ORDERBOT_SERVICE_REGION }}
      project_id: ${{ secrets.ORDERBOT_SERVICE_PROJECT_ID }}
      workload_identity_provider: ${{ secrets.ORDERBOT_SERVICE_WORKLOAD_IDENTITY_PROVIDER }}