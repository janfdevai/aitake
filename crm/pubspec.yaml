name: crm
description: "A modern CRM for the AIOTake platform."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_riverpod: ^2.4.4
  google_fonts: ^6.1.0
  intl: ^0.18.1
  uuid: ^4.1.0
  supabase_flutter: ^2.12.0
  flutter_dotenv: ^5.1.0
  image_picker: ^1.2.1
  url_launcher: ^6.3.0
  purchases_flutter: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/env

